const initialState = {
  breedList: {},
  catList: {},
  currentCat: {},
};

const state = () => (initialState);

const actions = {};

const mutations = {};

const getters = {};

export default {
  namespaced: true,
  mutations,
  actions,
  getters,
  state,
};
